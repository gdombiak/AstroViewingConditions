# Runtime: GDAL Python bindings (osgeo) must come from the system/Homebrew install.
# Use Homebrew python3: /opt/homebrew/bin/python3
# Verify: python3 -c "from osgeo import gdal; print(gdal.__version__)"
numpy>=1.26
pytest>=8.0
