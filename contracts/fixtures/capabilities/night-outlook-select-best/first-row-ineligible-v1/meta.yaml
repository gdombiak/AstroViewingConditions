capability: observing_night.select_best
engine_semver: ">=1.0.0 <2.0.0"
equality: night_outlook_best
origin: manual
hosts: [swift, python]
notes: Eligibility is applied before the reduction, so the earliest *eligible* row wins a tie.
