capability: observing_window.select
engine_semver: ">=1.0.0 <2.0.0"
equality: observing_window
hosts: [swift, python]
origin: manual
notes: >
  Add elapsed seconds through midnight without a local calendar.
