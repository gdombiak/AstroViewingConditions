capability: observing_window.select
engine_semver: ">=1.0.0 <2.0.0"
equality: observing_window
hosts: [swift, python]
origin: manual
notes: >
  The leading two-row run is considered when it terminates.
