capability: weather.decode
engine_semver: ">=0.1.0 <2.0.0"
equality: weather_decode
hosts: [swift, python]
origin: transcribed
notes: >
  Named Open-Meteo provider fixture providers/open-meteo/forecast/tz-from-offset-only.json.
  No timezone key; utc_offset_seconds -28800 is the normalization mechanism.
  timezone in the result is JSON null, not "UTC".
