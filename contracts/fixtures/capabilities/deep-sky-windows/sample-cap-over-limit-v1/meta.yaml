capability: targets.deep_sky_windows
engine_semver: ">=1.0.0 <2.0.0"
equality: deep_sky_windows
hosts: [swift, python]
origin: manual
notes: >
  One sample past the cap is rejected before the sampling loop runs; code is sample_cap, not validation.
