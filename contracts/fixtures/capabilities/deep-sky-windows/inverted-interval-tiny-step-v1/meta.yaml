capability: targets.deep_sky_windows
engine_semver: ">=1.0.0 <2.0.0"
equality: deep_sky_windows
hosts: [swift, python]
origin: manual
notes: >
  An inverted interval is not a cap: it emits no samples and does no work, mirroring location.grid where non-positive geometry is not a cap.
