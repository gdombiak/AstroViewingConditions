capability: targets.deep_sky_windows
engine_semver: ">=1.0.0 <2.0.0"
equality: deep_sky_windows
hosts: [swift, python]
origin: manual
notes: >
  An inverted interval yields no samples and therefore no windows; it is not an error.
