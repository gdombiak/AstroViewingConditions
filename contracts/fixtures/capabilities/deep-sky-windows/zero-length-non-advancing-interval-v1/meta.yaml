capability: targets.deep_sky_windows
engine_semver: ">=1.0.0 <2.0.0"
equality: deep_sky_windows
hosts: [swift, python]
origin: manual
notes: >
  Zero span makes the quotient 0, so only the advance check rejects this; without it the loop would hang on a single instant.
