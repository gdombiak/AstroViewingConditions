capability: targets.deep_sky_windows
engine_semver: ">=1.0.0 <2.0.0"
equality: deep_sky_windows
hosts: [swift, python]
origin: manual
notes: >
  An unknown target id is validation, not an empty window list; the host guard for non-deep-sky targets is composition.
