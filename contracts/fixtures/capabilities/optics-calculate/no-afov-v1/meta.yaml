capability: optics.calculate
engine_semver: ">=1.0.0 <2.0.0"
equality: optics_calculate
hosts: [swift, python]
origin: manual
notes: Apparent field is omitted, so approximate true field is unavailable.
