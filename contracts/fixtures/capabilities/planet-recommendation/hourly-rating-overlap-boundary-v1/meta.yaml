capability: targets.planet_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: planet_recommendation
hosts: [swift, python]
origin: manual
notes: >
  A rating ending exactly at the window start and one beginning exactly at the window end both fail the strict overlap test, so the cloud-cover fallback applies.
