capability: targets.planet_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: planet_recommendation
hosts: [swift, python]
origin: manual
notes: >
  Every optional branch is off at once; the always-appended moonlight code keeps the list non-empty.
