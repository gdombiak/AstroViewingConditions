capability: targets.planet_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: planet_recommendation
hosts: [swift, python]
origin: manual
notes: >
  A rating exactly on the rating floor is accepted, but its hour ends exactly at the window start, so the strict overlap test excludes it and the cloud-cover fallback applies.
