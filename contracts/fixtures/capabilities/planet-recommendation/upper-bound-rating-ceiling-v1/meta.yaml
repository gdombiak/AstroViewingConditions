capability: targets.planet_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: planet_recommendation
hosts: [swift, python]
origin: manual
notes: >
  A rating exactly on the rating ceiling is accepted. It begins exactly at the latest possible window end, so the strict overlap test still excludes it.
