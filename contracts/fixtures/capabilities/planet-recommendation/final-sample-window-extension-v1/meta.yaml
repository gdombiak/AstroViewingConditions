capability: targets.planet_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: planet_recommendation
hosts: [swift, python]
origin: manual
notes: >
  The run ends on the final sample, so the window extends by the fixed 900 s literal, not by the sampling cadence.
