capability: targets.planet_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: planet_recommendation
hosts: [swift, python]
origin: manual
notes: >
  One ULP-scale step below the threshold removes the only candidate, so there is no recommendation.
