capability: targets.planet_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: planet_recommendation
hosts: [swift, python]
origin: manual
notes: >
  The altitude change across the crossing segment is inside the 0.0001 degree interpolation guard, so the earlier instant is used unchanged.
