capability: targets.planet_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: planet_recommendation
hosts: [swift, python]
origin: manual
notes: >
  The earliest rating whose hour can still end after the earliest possible window start. It overlaps, so weather comes from the rating rather than the cloud-cover fallback.
