capability: night_conditions.classify_cloud_timing
engine_semver: ">=1.0.0 <2.0.0"
equality: cloud_timing
origin: manual
hosts: [swift, python]
notes: Rows are never sorted. Three heavy hours that would be contiguous once sorted are supplied out of order, so every step is a non-3600 delta and no run ever reaches two rows.
