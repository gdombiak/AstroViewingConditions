capability: night_conditions.classify_cloud_timing
engine_semver: ">=1.0.0 <2.0.0"
equality: cloud_timing
origin: manual
hosts: [swift, python]
notes: Two eligible runs: the three-hour run wins on length even though the two-hour run is cloudier.
