capability: observing_night.resolve_active
engine_semver: ">=1.0.0 <2.0.0"
equality: observing_night
origin: manual
hosts: [swift, python]
notes: With no hourly forecast the index is the bare day offset, so the preceding night can never resolve and day 0 is used whatever the reference date is.
