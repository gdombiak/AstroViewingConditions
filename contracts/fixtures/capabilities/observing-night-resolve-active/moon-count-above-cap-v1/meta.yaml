capability: observing_night.resolve_active
engine_semver: ">=1.0.0 <2.0.0"
equality: observing_night
origin: manual
hosts: [swift, python]
notes: daily_moon_count is a scalar out of domain, not a transported array overflow, so it is validation rather than sample_cap.
