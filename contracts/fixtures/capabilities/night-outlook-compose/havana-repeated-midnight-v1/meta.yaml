capability: observing_night.compose_outlook
engine_semver: ">=1.0.0 <2.0.0"
equality: night_outlook
origin: manual
hosts: [swift, python]
notes: A repeated local midnight: the day shift keeps the source offset, unlike startOfDay.
