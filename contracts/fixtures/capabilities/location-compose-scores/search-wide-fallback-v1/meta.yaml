capability: location.compose_scores
engine_semver: ">=1.0.0 <2.0.0"
equality: location_compose_scores
hosts: [swift, python]
origin: manual
notes: One scorable missing-LP row forces every survivor to Night Conditions.
