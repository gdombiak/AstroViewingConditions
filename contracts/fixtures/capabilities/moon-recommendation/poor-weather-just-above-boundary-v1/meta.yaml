capability: targets.moon_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: moon_recommendation
hosts: [swift, python]
origin: manual
notes: >
  The overlapping rating is chosen so the weather quality is the smallest binary64 value above 0.45. The poor threshold is strict, so poorWeather does not fire. No input reaches exactly 0.45: the v with 1 - v == 0.45 sits exactly between two doubles.
