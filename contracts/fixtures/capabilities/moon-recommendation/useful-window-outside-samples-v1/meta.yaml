capability: targets.moon_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: moon_recommendation
hosts: [swift, python]
origin: manual
notes: >
  A best conditions window disjoint from the samples leaves no useful samples at all, so the result is null. The window is used as given and is not clipped to the night.
