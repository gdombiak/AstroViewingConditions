capability: targets.moon_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: moon_recommendation
hosts: [swift, python]
origin: manual
notes: >
  An omitted best_window falls back to the astronomical night interval, which is the production bestWindow == nil path.
