capability: targets.moon_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: moon_recommendation
hosts: [swift, python]
origin: manual
notes: >
  A single visible sample: the window is that instant extended by the fixed 1800 s literal, well inside the useful end.
