capability: targets.moon_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: moon_recommendation
hosts: [swift, python]
origin: manual
notes: >
  The last visible sample is the interval end, so the +1800 s extension clips back to the useful end and the window is the interval.
