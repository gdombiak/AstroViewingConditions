capability: targets.moon_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: moon_recommendation
hosts: [swift, python]
origin: manual
notes: >
  Ratings exist but none of their [time, time+3600) spans overlaps the window, so the cloud fallback still applies.
