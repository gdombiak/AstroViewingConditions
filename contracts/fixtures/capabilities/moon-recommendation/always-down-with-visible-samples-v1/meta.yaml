capability: targets.moon_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: moon_recommendation
hosts: [swift, python]
origin: manual
notes: >
  always_down forces moonBelowUsefulWindow even when the injected samples are above the horizon; the flag comes from a different threshold than sample visibility.
