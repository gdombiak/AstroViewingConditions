capability: targets.moon_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: moon_recommendation
hosts: [swift, python]
origin: manual
notes: >
  The adjacent binary64 value below has quarter distance 0.08000000000000002, just outside the band, so mid illumination falls through to the 0.68 default. Both hosts must agree on that bit.
