capability: targets.moon_recommendation
engine_semver: ">=1.0.0 <2.0.0"
equality: moon_recommendation
hosts: [swift, python]
origin: manual
notes: >
  Unsorted rows are summed in caller order; production never sorts them and binary64 addition is order dependent.
