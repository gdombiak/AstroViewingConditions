capability: observing_quality.assess
engine_semver: ">=0.1.0 <2.0.0"
equality: observing_quality_anchor
hosts: [swift, python]
origin: manual
procedure: contracts/procedures/observing-quality.md
notes: Inclusive plausible-range minimum. In-range, so endpoint-clamp to first base penalty 8.0, not unavailable.

