capability: location.grid
engine_semver: ">=0.1.0 <2.0.0"
equality: grid
hosts: [swift, python]
origin: transcribed
notes: >
  High latitude uses the same great-circle destination as mid-latitudes; no separate polar path and no planar cosine grid.
