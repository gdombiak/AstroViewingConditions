capability: location.grid
engine_semver: ">=0.1.0 <2.0.0"
equality: grid
hosts: [swift, python]
origin: transcribed
notes: >
  Zero radius matches production generateGrid: empty array, not a validation error.
