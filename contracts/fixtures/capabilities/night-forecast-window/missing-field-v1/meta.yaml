capability: night_forecast.derive_window
engine_semver: ">=1.0.0 <2.0.0"
equality: night_forecast_window
origin: manual
hosts: [swift, python]
notes: The strict injected shape requires an explicit nullable tomorrow field.
