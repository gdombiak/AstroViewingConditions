capability: equipment.match
engine_semver: ">=1.0.0 <2.0.0"
equality: equipment_match
hosts: [swift, python]
origin: manual
notes: >
  Canonically equivalent Unicode instrument keys remain distinct; final key order uses UTF-8 bytes.
