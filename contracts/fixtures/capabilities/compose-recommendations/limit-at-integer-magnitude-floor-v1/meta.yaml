capability: targets.compose_recommendations
engine_semver: ">=1.0.0 <2.0.0"
equality: compose_recommendations
hosts: [swift, python]
origin: manual
notes: >
  The negative end of the shared integer-transport magnitude is accepted and
  still selects nothing.
