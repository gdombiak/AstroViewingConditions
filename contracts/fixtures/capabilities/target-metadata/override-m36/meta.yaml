capability: targets.requirements
engine_semver: ">=1.0.0 <2.0.0"
equality: target_requirements
hosts: [swift, python]
origin: manual
notes: >
  Exact whole-record override from production TargetEquipmentRequirements; omitted initializer fields retain model defaults.
